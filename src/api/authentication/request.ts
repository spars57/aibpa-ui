export type LoginRequest = {
    email: string;
    password: string;
};

export type RegisterRequest = {
    firstName: string;
    lastName: string;
    country: string;
    city: string;
    username: string;
    email: string;
    password: string;
};
